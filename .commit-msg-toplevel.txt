fix: top-level field type validation (5 fail-opens closed)

Closed 5 remaining fail-open cases by adding top-level field type validation
per Core schemas/contracts.v1.json at 15d1f8f9.

## Fixed Fail-Opens

All now return REJECT_UNKNOWN (were ACCEPT):
- allowed_capabilities = "read"   ✓ (must be array of strings)
- evidence_ids = {}               ✓ (must be array of strings)
- schema_version = "2"            ✓ (must be exactly "1")
- routing_decision = "x"          ✓ (must be null or plain object)
- created_at = 5                  ✓ (must be null or string)

## Changes

### Verifier (src/verifier.ts)
Added top-level field type checks in schema validation:
- Step 1d: schema_version must be exactly "1"
- Step 1e: policy_digest must match pattern ^[a-fA-F0-9]{64}$
- Step 1f: allowed_capabilities must be array of strings
- Step 1g: evidence_ids must be array of strings
- Step 1h: routing_decision must be null or plain object
- Step 1i: created_at must be null or string (if present)

### Tests
- Added 5 new cases to tests/fixtures/core-parity-cases.json
- Total differential cases: 21 (was 16)
- Total verifier tests: 39 (was 34)

## Differential Test Results

**32 total inputs tested (6 canonical + 21 differential + 5 garbage):**

All verdicts match Core Python verify_execution_envelope.

New cases (all REJECT_UNKNOWN):
- allowed_cap_str          → REJECT_UNKNOWN ✓
- evidence_ids_obj         → REJECT_UNKNOWN ✓
- schema_version_2         → REJECT_UNKNOWN ✓
- routing_dec_str          → REJECT_UNKNOWN ✓
- created_at_num           → REJECT_UNKNOWN ✓

## Verification

All checks passed:
- ✅ npm run build
- ✅ npm run lint
- ✅ npm run format:check
- ✅ npm run typecheck
- ✅ npm test (263 tests total, 39 verifier tests)
- ✅ npm run verify:package
- ✅ npm run consumer:smoke

**32/32 inputs agree with Core**
**0 fail-open cases remain**
**0 ACCEPT from garbage/adversarial inputs, 0 throws**
